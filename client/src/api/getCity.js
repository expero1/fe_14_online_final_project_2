// import { AppError } from '../errors/errors';
// import { NPcityEP } from './constants';
// import fetchApi from './fetchApi';

// const getCity = (cityName) => {
//   try {
//     return fetchApi(`${NPcityEP}?city=${cityName}`);
//   } catch (error) {
//     if (error instanceof AppError) {
//       throw AppError('NovaPoshta something went wrong');
//     }
//     throw error;
//   }
// };
// export default getCity;
